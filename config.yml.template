token:
  browser:
    # sec
    expires: 18000
    # refresh
    refresh: 36000
  backend:
    # min
    expires: 18000
    # refresh
    refresh: 36000
    # JWTトークンの署名に使用されるランダムな秘密鍵を生成(以下の鍵は例)
    # openssl rand -hex 32
    SECRET_KEY: '###########################################'
    # JWTトークンの署名に使用するアルゴリズム
    ALGORITHM: 'HS256'
